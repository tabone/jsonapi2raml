'use strict'

/**
 * Contains a list of objects that should be used as a prototype for RAML
 * Representational objects that describe the JSON-API Resources provided to
 * `jsonapi2raml` to create the RAML Documentation.
 * @type {Object}
 */
module.exports = {
  types: require('./types')
}
