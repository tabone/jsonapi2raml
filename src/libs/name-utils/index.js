'use strict'

/**
 * Utility used to return the name of RAML Objects that describe the JSON-API
 * Resources provided to `jsonapi2raml` to create the RAML Documentation.
 * @type {Object}
 */
module.exports = {
  types: require('./types')
}
